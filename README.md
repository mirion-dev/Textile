## Installation

1. Move the desired packages to `%APPDATA%/typst/packages/local`.
1. Import them using `#import "@local/<name>:<version>"`.

## Credits

- [theorion](https://github.com/OrangeX4/typst-theorion/) (uses their `richer-counters` only)
